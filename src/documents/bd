luizdsouza90
OMfjJcA4sr9Vmozv


//let newCar = await axios.post(req.headers["x-callback-url"], car)

//x-callback-url
//https://webhook.site/2f24ba9d-e8d9-40ab-a3b4-4adfce1f0e6e

/* 
import { HttpClientParams, HttpClient } from '@/infra/gateway'
import axios from 'axios'

export class AxiosHttpClient implements HttpClient {
  async post (request: HttpClientParams): Promise<any> {
    console.log(request)
    const { url, data, headers } = request
    const result = await axios.post(url, data, { headers })
    console.log(result.data)
    return result.data
  }
}
 */